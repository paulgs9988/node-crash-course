const {people} = require('./people');

const xyz = require('./people');

//console.log(xyz);

// console.log(xyz.people, xyz.ages);

// console.log('here is: ', people);

const os = require('os');   //'os' is built into node already

console.log(os.platform(), os.homedir());